global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - prometheus.rules.yml

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  - job_name: 'minitwit-api'
    static_configs:
      - targets: ['host.docker.internal:5034']
