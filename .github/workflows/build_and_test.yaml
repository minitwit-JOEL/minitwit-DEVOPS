name: build_and_test
on:
  workflow_dispatch:
  workflow_call:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build_and_test_NET:
    runs-on: ubuntu-latest
    environment: local_appsettings
    steps:
      - uses: actions/checkout@v4
      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: 8.0.*
      - name: Restore dependencies .NET
        run: dotnet restore
      - name: Build .NET
        run: dotnet build src/minitwit.Api --no-restore
      - name: Test .NET
        env:
          SIM_API_KEY: ${{ secrets.SIM_API_KEY }}
          TOKEN__KEY: ${{ env.TOKEN__KEY }} 
          TOKEN__ISSUER: ${{ env.TOKEN__ISSUER }}
          TOKEN__AUDIENCE: ${{ env.TOKEN__AUDIENCE }}
          CONNECTIONSTRINGS_DEFAULTCONNECTION: ${{ env.CONNECTIONSTRINGS_DEFAULTCONNECTION }}
        run: dotnet test tests/Integration/Integration.csproj --verbosity normal